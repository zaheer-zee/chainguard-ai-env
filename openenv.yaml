name: "chainguard-ai-env"
version: "1.0.0"
description: "A real-world supply chain security environment where the agent must triage and fix CVEs in Docker containers and package dependencies."
author: "AI Builder"
entry_point: "env:ChainGuardEnv"
dependencies:
  - "pydantic"
  - "pyyaml"
tasks:
  - id: "easy"
    score: 0.2
  - id: "medium"
    score: 0.3
  - id: "hard"
    score: 0.5
